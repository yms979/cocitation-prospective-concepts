# Copy to .env, fill in, then load with:   set -a; source .env; set +a
# .env is git-ignored. No key is stored anywhere in the source code.

# OpenAI - text-embedding-ada-002 (stages 3, 5, 6) and GPT chat models (stage 5 grammar
# correction, stage 6 judges and content baselines). Also used internally by vec2text.
OPENAI_API_KEY=

# Anthropic - Claude judge in src/06_evaluation/04_agent_as_a_judge_multimodel.py
ANTHROPIC_API_KEY=

# Google - Gemini judge in src/06_evaluation/04_agent_as_a_judge_multimodel.py
GOOGLE_API_KEY=
